# Getting Started with Create React Email

## Available Scripts

In the project directory, you can run:

### `yarn dev`

Runs a development mode to build the email, run a local server, and\ watch for changes. It builds and serves out of the `public` folder.

### `yarn build`

Builds the email for production to the `public` folder.
